apiVersion: v1
kind: Service
metadata:
    name: "devaks-40ac"
    labels:
        app: "devaks-40ac"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "devaks-40ac"