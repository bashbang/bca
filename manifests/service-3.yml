apiVersion: v1
kind: Service
metadata:
    name: "devaks-e617"
    labels:
        app: "devaks-e617"
spec:
    type: LoadBalancer
    ports:
    - port: 1337
      targetPort: 1337
      protocol: TCP
      name: http
    selector:
        app: "devaks-e617"