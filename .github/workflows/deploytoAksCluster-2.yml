on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    
    - uses: azure/docker-login@v1
      with:
        login-server: devlo3ooofmacr.azurecr.io
        username: ${{ secrets.acr_devlo3ooofmacr_username }}
        password: ${{ secrets.acr_devlo3ooofmacr_password }}
    
    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/strapi" -f  "strapi/Dockerfile" -t devlo3ooofmacr.azurecr.io/devaks:${{ github.sha }}
        docker push devlo3ooofmacr.azurecr.io/devaks:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      with:
         kubeconfig: ${{ secrets.aks_devaks_kubeConfig }}
      id: login
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep development | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace development`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: development
        container-registry-url: devlo3ooofmacr.azurecr.io
        container-registry-username: ${{ secrets.acr_devlo3ooofmacr_username }}
        container-registry-password: ${{ secrets.acr_devlo3ooofmacr_password }}
        secret-name: devaksdockerauth
       
    - uses: azure/k8s-deploy@v1.3
      with:
        namespace: development
        manifests: |
          manifests/deployment-2.yml
          manifests/service-2.yml
        images: |
          devlo3ooofmacr.azurecr.io/devaks:${{ github.sha }}
        imagepullsecrets: |
          devaksdockerauth